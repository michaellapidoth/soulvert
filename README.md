## Prerequisite

This tool calls the standalone **Soulseek CLI**.
Install it first:

```bash
brew install soulseek-cli             # macOS
# or
pipx install "git+https://github.com/aeyoll/soulseek-cli.git"
